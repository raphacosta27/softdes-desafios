site_name: Servidor de Desafios

theme:
  name: "material"

site_url: "http://rmp.github.io/serv"

plugins:
  - search
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          rendering:
            show_source: true
      watch:
        - ../src/